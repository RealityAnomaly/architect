# Architect Wrapper

A small wrapper to invoke architect in the current directory with a default permission set.

To install, run `deno install --global -A jsr:@glassway/architect-wrapper`.

Or for development: `deno install --global -A src/architect.ts -f`
