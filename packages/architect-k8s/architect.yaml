library: true
backend:
  type: file
plugins:
  kubernetes:
    crds:
      - name: fluxcd.io
        http:
          paths:
            - >-
              https://github.com/fluxcd/flux2/releases/latest/download/install.yaml
