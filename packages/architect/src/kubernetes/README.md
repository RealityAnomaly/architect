# Kubernetes model logic

This directory contains logic to handle Kubernetes data structures, which we use in Architect core code for configuration management (for example, Target is a CRD).

The rest of Kubernetes support for Architect lives in `architect-k8s`.
