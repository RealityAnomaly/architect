import { IIoK8sApiDiscoveryV1EndpointConditions } from "./EndpointConditions.ts";
import { IIoK8sApiDiscoveryV1EndpointHints } from "./EndpointHints.ts";
import { IIoK8sApiCoreV1ObjectReference } from "../../v1/ObjectReference.ts";
import { ModelData, Model, setValidateFunc } from "@glassway/architect/kubernetes/types/model";
import { ValidateFunc } from "@glassway/architect/kubernetes/validate";
import { validate } from "../../_schemas/IoK8sApiDiscoveryV1Endpoint.js";

/**
 * Endpoint represents a single logical "backend" implementing a service.
 */
export interface IEndpoint {
  /**
 * addresses of this endpoint. For EndpointSlices of addressType "IPv4" or "IPv6", the values are IP addresses in canonical form. The syntax and semantics of other addressType values are not defined. This must contain at least one address but no more than 100. EndpointSlices generated by the EndpointSlice controller will always have exactly 1 address. No semantics are defined for additional addresses beyond the first, and kube-proxy does not look at them.
 */
"addresses": Array<string>;
/**
 * conditions contains information about the current status of the endpoint.
 */
"conditions"?: IIoK8sApiDiscoveryV1EndpointConditions;
/**
 * deprecatedTopology contains topology information part of the v1beta1 API. This field is deprecated, and will be removed when the v1beta1 API is removed (no sooner than kubernetes v1.24).  While this field can hold values, it is not writable through the v1 API, and any attempts to write to it will be silently ignored. Topology information can be found in the zone and nodeName fields instead.
 */
"deprecatedTopology"?: {
  [key: string]: string;
};
/**
 * hints contains information associated with how an endpoint should be consumed.
 */
"hints"?: IIoK8sApiDiscoveryV1EndpointHints;
/**
 * hostname of this endpoint. This field may be used by consumers of endpoints to distinguish endpoints from each other (e.g. in DNS names). Multiple endpoints which use the same hostname should be considered fungible (e.g. multiple A values in DNS). Must be lowercase and pass DNS Label (RFC 1123) validation.
 */
"hostname"?: string;
/**
 * nodeName represents the name of the Node hosting this endpoint. This can be used to determine endpoints local to a Node.
 */
"nodeName"?: string;
/**
 * targetRef is a reference to a Kubernetes object that represents this endpoint.
 */
"targetRef"?: IIoK8sApiCoreV1ObjectReference;
/**
 * zone is the name of the Zone this endpoint exists in.
 */
"zone"?: string;
}

/**
 * Endpoint represents a single logical "backend" implementing a service.
 */
export class Endpoint extends Model<IEndpoint> implements IEndpoint {
  "addresses": Array<string>;
"conditions"?: IIoK8sApiDiscoveryV1EndpointConditions;
"deprecatedTopology"?: {
  [key: string]: string;
};
"hints"?: IIoK8sApiDiscoveryV1EndpointHints;
"hostname"?: string;
"nodeName"?: string;
"targetRef"?: IIoK8sApiCoreV1ObjectReference;
"zone"?: string;

constructor(data?: ModelData<IEndpoint>) {
  super();

  this.setDefinedProps(data);
}
}

setValidateFunc(Endpoint, validate as ValidateFunc<IEndpoint>);

export type {
  IEndpoint as IIoK8sApiDiscoveryV1Endpoint,
  Endpoint as IoK8sApiDiscoveryV1Endpoint
};
